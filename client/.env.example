# Copy to .env.production.local for production builds.
# Not needed for local development (app uses http://localhost:8080).

# Base URL of the Nest.js API (Cloud Run service URL). No trailing slash.
REACT_APP_API_URL=https://REGION-PROJECT_ID.cloudfunctions.net
REACT_APP_STRIPE_PUBLISHABLE_KEY=key-to-stripe

# To run locally:
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
