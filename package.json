{
  "name": "site-casamento",
  "version": "1.0.0",
  "description": "Site de casamento com React.js (TypeScript) e Firebase Functions",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../functions && npm install",
    "build": "cd client && npm run build && cd ../functions && npm run build",
    "dev": "./run-local.sh",
    "start": "./run-local.sh",
    "deploy": "./deploy.sh",
    "deploy:hosting": "./deploy.sh --hosting-only",
    "deploy:functions": "./deploy.sh --functions-only",
    "deploy:firestore": "./deploy.sh --firestore-only",
    "deploy:all": "./deploy.sh"
  },
  "keywords": ["casamento", "wedding", "react", "typescript", "firebase"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
