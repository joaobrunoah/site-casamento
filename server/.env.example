# Copy to .env or .env.local for local development.
# For production deployment, copy to .env.prod (used by deploy script when deploying to Cloud Run).

# Required: credentials for admin / basic-auth API endpoints
ADMIN_USER=your-admin-username
ADMIN_PASSWORD=your-admin-password

# For production
FRONTEND_URL=https://your-frontend-url.com

# Mercado Pago Checkout Pro (required for payment)
# Get your Access Token from: https://www.mercadopago.com.br/developers/panel/app
MERCADOPAGO_ACCESS_TOKEN=your-mercado-pago-access-token
# Required for webhook: secret signature from Your integrations > Webhooks (validate notification origin)
MERCADO_PAGO_SECRET_SIGNATURE=your-mercado-pago-secret-signature

# Optional: URL pública do backend para o Mercado Pago chamar quando o pagamento for aprovado/recusado.
# Configure também em Mercado Pago: Your integrations > Webhooks > Payments. Ex: https://sua-api.run.app/payment/webhook
# NOTIFICATION_URL=

# Optional: port the server listens on (default: 8080). Cloud Run sets PORT in production.
# PORT=8080

# Optional: only for local dev with Firestore emulator. run-local.sh sets this automatically.
# FIRESTORE_EMULATOR_HOST=localhost:8081

# Optional: only if running locally against real Firestore (not emulator). Cloud Run sets this automatically.
# GCP_PROJECT=your-firebase-project-id
